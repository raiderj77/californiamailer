rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // All application files are private and served only by owner/reservation
    // route handlers using the Admin SDK. Browser SDK access is denied.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
